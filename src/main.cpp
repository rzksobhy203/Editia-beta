#include <edpch.h>

int main(void)
{
    // initializing our logging system
    // log defines
    // LOG_INFO(...)
    // LOG_WARN(...)
    // LOG_ERROR(...)
    // LOG_TRACE(...)
    editia::Log::init();

    
	return 0;
}
